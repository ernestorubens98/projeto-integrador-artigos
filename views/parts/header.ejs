<header>
    <nav class="d-flex navbar-dark bg-darkOver2 border-bottom border-secondary align-items-center">

        <a class="navbar-brand align-items-center ml-2" href="/">
            <!-- <span class="align-text-middle">REVIEW ARTICLE</span> -->
            <h1 class="align-text-middle h1-login">REVIEW ARTICLE</h1>
        </a>

        <form class="d-flex w-50 ml-1" action="/artigo/busca/" method="GET">
            <input class="form-control text-light w-75" name="titulo" type="search" placeholder="Procurar" aria-label="Search" value="">
            <button class="btn btn-outline-info ml-2 botao-procurar" type="submit">Procurar</button>
        </form>
        <div>
            <% if (typeof(usuario) !== 'undefined') { %>
                <h5>Olá <%= usuario.nome%> </h5>
            <% } %>
        </div>
        <div class="nav-item d-flex align-items-center ml-auto">
            <a href="/artigo/escreva-artigo" type="button" class="btn btn-primary escreva-artigo">
                <span class="h5">Escreva um Artigo</span>
            </a>
            <% if (typeof(usuario) == 'undefined') { %>
                <a class="nav-link my-2 about-login" href="/usuario/login">Login</a>
            <% } else { %>
                <a class="nav-link my-2 about-login" href="/usuario/logout">Logout</a>
            <% } %>
        </div>

    </nav>
</header>